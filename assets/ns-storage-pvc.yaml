# PVC references StorageClass "fast-ssd" which does not exist.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-data
  namespace: ns-storage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-ssd
  resources:
    requests:
      storage: 1Gi
